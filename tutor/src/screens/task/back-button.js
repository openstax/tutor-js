import { styled } from 'vendor';
import BackButton from '../../components/buttons/back-button';

const CenteredBackButton = styled(BackButton)`
margin-top: 3rem;
align-self: center;
`;

export { BackButton, CenteredBackButton };
