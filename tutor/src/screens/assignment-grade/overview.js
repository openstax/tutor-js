import { React, PropTypes, observer, styled } from 'vendor';

const Overview = observer(({ ux }) => {
  return (
    <>
      {
        ux.headings.map(h => {
                    
        })
      }
    </>
  );

});
