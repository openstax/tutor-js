#!/bin/bash

set -e

bin/checkinputs "$@"

DIR=$(dirname "$0")

export OX_PROJECT=$1
echo Test: $OX_PROJECT

if [[ $2 = "integration" ]]; then
  $DIR/test-integration $OX_PROJECT ${@:3}
else
  $DIR/test-unit $OX_PROJECT ${@:2}
fi
